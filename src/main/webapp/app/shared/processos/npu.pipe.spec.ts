/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NpuPipe } from './npu.pipe';

describe('Pipe: Npue', () => {
  it('create an instance', () => {
    let pipe = new NpuPipe();
    expect(pipe).toBeTruthy();
  });
});
